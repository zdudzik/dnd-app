<%= form_for(kevin) do |f| %>
  <% if kevin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kevin.errors.count, "error") %> prohibited this kevin from being saved:</h2>

      <ul>
      <% kevin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :occupation %>
    <%= f.text_field :occupation %>
  </div>

  <div class="field">
    <%= f.label :age %>
    <%= f.number_field :age %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
